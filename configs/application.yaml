server:
  name: go-service
  port: 8080

sql:
  uri: devadmin:abcd1234@/masterdata?charset=utf8&parseTime=True&loc=Local

log:
  level: debug
  field_map:
    time: "@timestamp"
    msg: message

middleware_log:
  single: true
  status: status
  bytes: bytes
  request: request
  response: response
